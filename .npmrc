# npm 12+ defaults allow-git=none.
# Hub pins @fabric/core and @fabric/http from GitHub; http also depends on core
# as git. Nested `npm install` during git dep preparation resolves those to
# commit SHAs — `allow-git=root` refuses that fetch. Use `all` for this stack.
allow-git=all
