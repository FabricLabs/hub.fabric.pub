# Development Plan
## P0
- [x] basic peering
  - [x] fabric 7777 (`FABRIC_PORT`, Peer listen; seeds + `/services/peering` claim)
  - [ ] http 80 (prod still defaults HTTP `8080` behind TLS proxy; set `FABRIC_HUB_PORT=80` or terminate at edge)
  - [x] webRTC coordinator served from Hub instances at `/services/peering` (+ `RegisterWebRTCPeer` / `ListWebRTCPeers`)
- [ ] global messaging
  - [ ] global chat published as part of registry contract
    - [ ] provides encrypted-by-default basis
    - [ ] can be regenerated by the local developer as the initial contract publisher, later delegated to Federation
  - [x] secure to relay snooping (encrypted across hops) — onion chat seal on Hub `SendOnion` text + Peer open (`@fabric/core/functions/onionChatSeal`); mesh flood chat still cleartext
- [~] document market (aggregate gossip -> expose offers -> make profit) — priced `DocumentPublish` + inventory HTLC + L1 block/tx docs shipped; gossip aggregation / offer surface still incomplete
- [x] adversarially ready
  - [x] SECURITY.md + `tests/adversarialEnvironment.basics.test.js` (hub allowlist / faucet non-advertise)
  - [ ] full mesh adversarial suite beyond basics
## P1
- [ ] mainnet settlement
- [ ] reduce default UI to only core features, simple language
## P2
- [ ] break out hidden features to dedicated UI on select integrations
## P3
