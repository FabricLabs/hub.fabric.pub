# Changelog
All notable changes to **hub.fabric.pub** (Fabric Hub) are documented here. RCs are coordinated with **`@fabric/core`** and **`@fabric/http`**.

## [Unreleased]
- **Mocha HTTP bind:** under mocha / `NODE_ENV=test`, Hub forces Peer + HTTP listen to `127.0.0.1` and `peersDb: null` so deploy-host env (`FABRIC_HUB_INTERFACE`, `FABRIC_INTERFACE`, `FABRIC_HUB_PEERS_DB`) cannot make `npm test` ECONNREFUSED on loopback. Production seed hosts leaked through `settings/local.js` are stripped from `peers` (opt back in with `FABRIC_TEST_ALLOW_DEFAULT_PEERS=1`). Opt out of loopback with `FABRIC_TEST_ALLOW_HOST_HTTP_BIND=1` / `FABRIC_TEST_ALLOW_HOST_PEERS_DB=1`. `hub.start()` waits until the Node HTTP socket is listening. Lightning HTTP tests use ephemeral ports.
- **Peers list:** Fabric peers (`GetNetworkStatus` / Peers page) are ordered most recently seen first (`lastSeen` / `lastMessage`), not by primary hub or score.
- **FederationContractInvite expiry:** Hub-built co-signer invites inherit `@fabric/http`
  `expiresAt` (default 7 days after `invitedAt`). Wrapper probes the pin and collapses
  to the bare http module once it stamps; fallback `stampExpiresAt` rejects non-positive
  `invitedAt` (`Number(null)===0`).
- **Bridge client ids:** optimistic chat / wallet / WebRTC session suffixes use
  `crypto.getRandomValues` (no `Math.random`).
- **Lifted HTTP APIs:** `tests/liftedApis.exports.test.js` compares `chatTextOf` by result, not function identity — nested npm copies of `@fabric/core` under `@fabric/http` are distinct objects after `npm link @fabric/core`.
- **AMP `parent` (D-020):** `_appendFabricMessage` sets header `parent` to the previous signed `Message.id` before `signWithKey` / `fs.addToChain`. Ping / Pong stay genesis zeros (`GENESIS_MESSAGE` is the chain root). JSON log rows carry `parent` and `frameId`. Inbound zeros are still accepted. Needs core `functions/fabricMessageParent` (pin [#186](https://github.com/FabricLabs/fabric/pull/186) / linked tree).
- **PR #16 review:** Hub-level Accept/Reject tests cover tokens signed by `_rootKey` **and** `agent.key` (`tests/hub.operatorAccept.test.js`), including Accept when `setup.verifyAdminToken` is false. Playnet `loadFabricHomeEnv` only swallows `MODULE_NOT_FOUND` (same as `scripts/hub.js`). Remaining CodeRabbit nits from #16 are already in tree (bulk-advisory arrays, `EditDocument` filter, resync warn, Uint8Array JSON parse). Wontfix: throw at Hub startup when the shared-mode WS token is unset; drop the advisory-detector Semgrep exclude; deploy `passport-privacy.html` is ops.
- **Core [#186](https://github.com/FabricLabs/fabric/pull/186):** next `@fabric/core` pin after [#185](https://github.com/FabricLabs/fabric/pull/185). HEAD **`9c6ade0`** (gossip catalog, handshake-bus wrapper, effective NUMS `internalKeyMode`, numeric relay-as-is, NOISE `freeNative` pointer clear). Live Hub still pins **`f63a33f`** — do not claim RSS/NOISE is fixed until this Hub lockfile bumps and playnet redeploys.
- **Core features L1 E2E:** `npm run test:e2e-core-features-l1` runs a three-hub gossip mesh on isolated regtest: native `fabric-beacon` registry, satellite `CONTRACT_PUBLISH` → Accept → contract Statechain patches + `CONTRACT_MESSAGE` rounds, Fabric hallmark OP_RETURN (`c0d3f33d`) as the on-chain contracts digest, then document-market unpriced inventory/`SendPeerFile` plus priced P2TR inventory HTLC with `VerifyBitcoinL1Payment`. A second test attaches WebRTC spokes (WebSocket AMP + `RegisterWebRTCPeer`) to each hub: same-hub `SendWebRTCSignal`, spoke-origin `RelayFromWebRTC` chat + `CONTRACT_MESSAGE` across the mesh, and a spoke-origin document fetched by another spoke. Opt-in (`FABRIC_CORE_FEATURES_L1_E2E=1`); needs `bitcoind`. Hallmark `walletcreatefundedpsbt` pins `fee_rate: 1` sat/vB so empty-mempool estimates cannot exceed Core’s max fee.
- **Payjoin L1 E2E:** `npm run test:e2e-payjoin-l1` funds a Hub-allocated deposit address on isolated regtest, verifies the output, then accepts a BIP78-shaped proposal. Session-only (Bitcoin off) remains `test:e2e-payjoin`. UI hooks: `data-testid="hub-payjoin-deposit"` / `hub-payjoin-board`.
- **Crowdfund regtest:** `test:crowdfund-regtest` POSTs `/settings` before waiting on bitcoind (same setup-deferral as inventory HTLC), keeps isolated extras via `hubSettingsMerge`, and confirms txs via wallet `gettransaction` when `getrawtransaction` cannot see them. Crowdfunds page: `data-testid="hub-crowdfund-page"`.
- **L1 payment verify:** `_l1PaymentVerificationDetail` falls back to wallet `gettransaction` + `decoderawtransaction` when `getrawtransaction` cannot see a confirmed wallet send (Core’s `-5` / “use gettransaction” path). Isolated inventory HTLC E2E hits this after POST `/settings` starts managed Core. `npm run test:e2e-inventory-htlc` (`tests/hub.inventory.htlc.e2e.test.js`) runs two isolated hubs: seller managed regtest + priced sealed publish, buyer `RequestPeerInventory` with a refund pubkey, miner wallet funds the P2TR quote, `ConfirmInventoryHtlcPayment` delivers ciphertext. Opt-in (`FABRIC_INVENTORY_HTLC_E2E=1`); needs `bitcoind`. Offer-book rows now keep a compact HTLC quote (no preimage). PeerView inventory HTLC panel has `data-testid="peer-inventory-htlc"`. Hub inventory request/response now use first-class `P2P_INVENTORY_*` AMP opcodes (Core Peer drops those types on GenericMessage). First-time setup keeps constructor `bitcoinExtraParams` (isolated tests need `-maxtxfee=10` after POST `/settings`).
- **Store reset:** `npm run reset:stores` (`scripts/reset-hub-stores.js`) wipes CLI `stores/hub`, desktop Fabric Hub + legacy Electron `stores/hub`, and (default) Bitcoin/Lightning datadirs + logs. `--setup-only` leaves chain data; `--dry-run` prints paths.
- **Bitcoin setup:** First-time Bitcoin step uses exclusive accordion groups (Network, Bitcoin Core, Lightning, Node binaries). Managed nodes default **transaction relay off** (`BITCOIN_TXRELAY`, Core `-blocksonly=1`). Lightning still forces relay + txindex + no prune. Older STATE without the key keeps Core’s default (relay on).
- **Tray / run at startup:** Packaged Hub desktop registers an OS login item (macOS / Windows) or XDG autostart (Linux) so Bitcoin, Fabric, and Lightning come up with the user session. Tray **Run at startup** is the control; login launches use `--hidden` (window stays in the tray). Unpackaged `npm run desktop` leaves login items off unless `FABRIC_OPEN_AT_LOGIN=1`. OS scripts ship in `scripts/startup/` → installer `resources/startup/`.
- **electron-builder 26 Linux `.desktop`:** `build.linux.desktop` is `{ entry: { Name, Comment } }` (v26 `LinuxDesktopFile`). A flat `Name`/`Comment` object fails schema validation and aborts `npm run build:desktop`.
- **Header:** Downloads is no longer a primary nav tab. `/downloads` still serves the FileBrowser (`assets/downloads/index.json`).
- **Hub toasts:** Bridge no longer pops `Hub: peer not connected` for expected misses (inventory probes / chat flush against WebRTC-only or disconnected known peers). Document inventory RPC only targets live Fabric TCP sessions; mesh inventories stay on the WebRTC data channel.
- **NOISE handshake telemetry:** `[HUB:HEAP]` includes `retainers.noiseHandshakeListeners.{write,read,split}` from `Peer#countNoiseHandshakeListeners` (null when the agent pin omits the method). Live **`6bf825d`** / core **`f63a33f`** still log **null** ([scan 2026-08-20T10:39Z](https://relay.goon.vc/downstream.agents.md)); RSS now tracks **`external`/`arrayBuffers`**, not named retainers. Pair with the core handshake-bus pin before treating flat Filesystem/STATE counters as “OOM solved”.
- **Desktop first-time setup store:** Unpackaged `npm run desktop` no longer keeps Hub `STATE` in Chromium’s **Electron** userData folder. The shell pins `app.setName('Fabric Hub')` and `userData` to `~/Library/Application Support/Fabric Hub` (macOS) before the single-instance lock, migrates `stores/` from the old Electron profile when the new folder is empty, logs the store path, and exposes tray **Reveal Hub data folder**. `rm -rf stores/hub` in the git checkout does not reset desktop onboarding.
- **Bitcoin Core pin:** Managed downloads use **29.4** (latest 29.x as of 2026-07-13) from bitcoincore.org `SHA256SUMS`, not Core 30/31. Core Lightning remains **26.06.6**.
- **Bitcoin setup binary checks:** Onboarding Node binaries list has **Check remote** (top right). `POST /services/binaries/check` downloads bitcoincore.org `/bin/` + pin `SHA256SUMS` and GitHub CLN latest, compares them to Hub pins (Core **29.4**, CLN **26.06.6**) and local installs, and downloads the pin only when a binary is missing (progress on GET `/services/binaries` `job`). Newer publisher listings are shown, not installed.
- **First-time setup stages:** Onboarding is Welcome → Node → Bitcoin. Bitcoin uses beginner Core knobs from Lopp’s Config Generator (preset, listen, prune/txindex, dbcache, maxconnections, maxuploadtarget). Completing setup writes `stores/hub/STATE` `.settings` (not `settings.json`), seeds collections + `peers/`, and keeps a spinner for at least 2.5s until `GET /settings` reports configured. CLI: `rm -rf stores/hub` restarts the flow; desktop: wipe userData `stores/hub`. Managed bitcoind is deferred until that completes.
- **Linux `.desktop` association:** Root `desktopName` is `pub.fabric.hub` (same as `appId` / `WM_CLASS`) with `build.linux.syncDesktopName`. GNOME/KDE can match the running window to the installed launcher.
- **Fabric lettermark:** Serif lowercase **f** (Arvo Bold) in white on royal purple `#4C1D95` for favicons (`assets/favicon.svg` / `.ico`) and desktop `build/icon.{icns,ico,png}`. Generator: `@fabric/http` `npm run make:icons`.
- **Desktop asar boot:** electron-builder now packs `contracts/**/*` and root `constants.js`. Packaged `settings/local.js` requires `contracts/beaconFederation` (plus liquid / resources); Hub requires `constants.js`. The previous installer crashed on launch with `Cannot find module '../contracts/beaconFederation'`.
- **`/downloads` FileBrowser:** Public SPA route browses build-generated `assets/downloads` via `index.json`. Folders stay in the app (`/downloads/mac`); files are same-origin static (`/downloads/mac/….dmg`). `npm run build` writes the index; `npm run build:desktop` / `build:installers` copy electron-builder artifacts from `dist/` afterward. Installer blobs are gitignored and excluded from the Electron asar.
- **Managed node binaries:** First-time setup downloads pinned **Bitcoin Core 29.4** and **Core Lightning 26.06.6** (Linux x86_64 tarball; macOS Homebrew; Windows Bitcoin-only) into `binaries/<platform>-<arch>/`. Hub defers bitcoind until those files exist, then prepends them to `PATH`. `GET|POST /services/binaries`; CLI `npm run binaries:fetch` / `binaries:fetch:all`. Cross-platform fetch looks for `bitcoind.exe` on Windows ids (so `build:installers` on macOS no longer fails after extracting the win64 zip) and does not treat a host `lightningd` as Linux Lightning. Desktop installers pack the matching tree via extraResources. **`npm run build:installers`** builds mac + Windows + Linux after prefetching every platform.
- **Federation vault NUMS (H2):** Hub defaults `internalKeyMode` to **`nums`** so historical NUMS UTXOs stay on the displayed address after `@fabric/core` #185 (n≥2 default is MuSig2). Env **`FABRIC_FEDERATION_INTERNAL_KEY_MODE`** (or `settings.federation.internalKeyMode`) — set `musig2` only after sweeping. Beacon genesis omits the field; overlay at Accept matches the vault. Manifest / Federation panel show the mode.
- **CDN seed nodes:** `window.FABRIC_HUB_SEEDS` / `FABRIC_HUB_SEEDS` lists Hub HTTP origins for HTML-only builds. The SPA probes `OPTIONS /` (feature detection), `GET /services/peering` (Fabric peering advertisement), uses the first reachable Hub for WebSocket/WebRTC signaling, and lists Documents from peer inventories rather than a local Hub catalog.
- **HTML SSR after webpack:** `scripts/build.js` no longer passes the just-run webpack config (circular `Compiler.root`) or `settings/local.js` into Fabric `Compiler`/`Actor`. SPA HTML compile uses `skipWebpack` after `browser.min.js` is already emitted.
- **HTML-only / CDN client chrome:** `GET /settings` with HTML, 404, or CDN protection JSON classifies the origin as a browser client (`functions/hubClientEnvironment.js`). Home, Settings, and local wallet stay; operator surfaces wait behind a Hub HTTP gate. Live Hub JSON keeps the full operator UI. Seed lists (`FABRIC_HUB_SEEDS`) enable OPTIONS detection, WebRTC mesh, and inventory-backed Documents on CDN.
- **PR #16 SECURITY-RECOMMENDATIONS fruit:** unauthenticated `GET /settings` redacts `BITCOIN_PASSWORD`. Live Lightning mutations and Hub-wallet Payjoin address allocation require the admin Bearer (stub Lightning stays open for tests). Production docs recommend loopback HTTP behind the proxy.
- **PR #16 Codacy gate:** `hubFilesystemPersist` JSDoc uses a normal space (`3.7 GiB`) so `no-irregular-whitespace` does not fail the PR. `lightningdOnPath` uses `which`/`where` instead of `path.join` + `fs.existsSync` on PATH entries (Semgrep/Opengrep also exclude that helper; Codacy ignores `nosemgrep`). Desktop userData / login-item, `/downloads` index, managed bitcoind fetch, and CDN seed `OPTIONS` probes are excluded the same way (path.join / pinned-URL false positives). Shared-mode listen-host reads named `FABRIC_HUB_INTERFACE` / `INTERFACE` / `FABRIC_HTTP_INTERFACE` properties instead of `env[key]`.
- **Heap telemetry (log-only):** periodic single-line `[HUB:HEAP] {json}` reports RSS/heap, V8 heap stats, and retainer map sizes (activity, fabric messages, documents catalog, offers, peers, tip set, last `STATE` byte length from `commit()`). Cadence defaults to Beacon interval (10 min; floor 60 s). Env: `FABRIC_HUB_HEAP_TELEMETRY=0` to disable, `FABRIC_HUB_HEAP_TELEMETRY_MS` to override. Does **not** truncate durable `messages/` / documents / STATE — full replay unchanged. Agents parse PM2 stdout. Tests: `tests/hub.heapTelemetry.test.js`.
- **PR #16 review nits:** `report:install` removes `package-lock.json` (instead of writing a blank line into it). Heap cap helpers return a trimmed copy instead of `delete map[key]`. Message-log restore basename-only reads + `parseFilesystemJson` for STATE. Docs record lockfile `@fabric/core` **`9938917`** / `@fabric/http` **`7d7f1c7`**.
- **Boot noise (not the heap root):** managed Lightning is opt-in (`lightning.managed` / `lightning.enable`, plus `lightningd` on PATH). `scripts/hub.js` datadir-only settings no longer spawn CLN (10 sock retries per life). Ops: `pm2-logrotate`, drop dead `sensemaker.io:7778` seed, Hub Node **24.15.x** — [PRODUCTION.md](docs/PRODUCTION.md).
- **Heap OOM (desktop Electron):** `npm run desktop` loads `node_modules/@fabric/core` (not a `link:fabric` tree). That pin’s `Filesystem.publish` still retains every body and `synchronize()`s. Hub now wraps `fs.publish` → `writeFile` (`functions/hubFilesystemPersist.js`) so Payjoin, federation registry, contract queue, identity cluster, and document writes cannot retain. Tip indexing uses one `getblock` (verbosity **1** unless tx documents or regtest federation scan need **2**) instead of two verbosity-2 calls. Local crash: ~844 s / ~3671 MiB mark-compact; Beacon never started (`beacon/NETWORK` guard); `sensemaker.io:7777` timeout is a dead seed, not the leak. Restart desktop after this; `npm run link:fabric` still picks up the core Filesystem cut. Do not raise `--max-old-space-size`.
- **Heap OOM (playnet Hub):** `Filesystem.publish` no longer retains bodies or re-syncs the tree (core). Hub `commit()` writes `STATE` with `writeFile` (not `publish`), stringifies `_state.content` after capping Activity (`256`) and the in-memory Fabric message map (`2048`). Merkle leaves are payload hashes. P2P BitcoinBlock ingest uses a bounded tip set. Oversized `STATE` snapshots skip JSON restore and reload a window from `messages/*.json`. Pins `992ea478` / `f1b5e147` / `1b162e35` were already live and did not stop the loop. Do not raise `--max-old-space-size`. Tests: `tests/hub.heapBounds.test.js`.
- **GetDocument Buffer JSON:** `Filesystem.readFile` returns a Buffer; Hub now JSON-parses Buffers (same as strings, including `Uint8Array`) so held files expose `contentBase64`. The old `typeof raw === 'string'` branch returned the Buffer as the document, which made document-network / fabric-epic e2e wait helpers report `no document`. Wait diagnostics treat an empty-string `contentBase64` as a payload mismatch, not a missing body.
- **Shared-mode http fallbacks:** when `@fabric/http/functions/httpSharedMode` omits `isHttpSharedModeEnabled` / `resolveHttpListenHost` / `DEFAULT_HTTP_LISTEN_ENV_KEYS`, Hub uses the local implementations (not only `applySharedModeWebsocketGate`). Local listen-host matches http: constructor `host` beats inherited `INTERFACE`.
- **BIP-69 PSBTs:** unsigned Hub builders sort vin/vout with `@fabric/core/functions/bip69` before the first signature (crowdfund payout, original Payjoin PSBT, Bitcoin `_buildPSBT`). Payjoin ACP still appends the Hub input and does not reorder signed slots.
- **Operator Accept:** `AcceptTrackedApplicationContract` / `RejectTrackedApplicationContract` verify Schnorr `OP_IDENTITY`/`admin` tokens against Hub `_rootKey` **or** the Fabric Peer identity (`agent.key`). Local developer `FABRIC_XPRV` is the production publisher. Missing token → `adminToken required`; bad signature → `adminToken invalid`. Hub-level tests bypass `setup.verifyAdminToken` (`tests/hub.operatorAccept.test.js`).
- **Peer / Bitcoin debug flood:** do not `console.debug` every Fabric Peer `debug` event, and do not `console.log` Bitcoin `debug`, unless `settings.debug` / `FABRIC_HUB_DEBUG`. Truncate at 2 KiB. Do **not** force `bitcoin.settings.debug = true` after start (that re-enabled per-RPC debug strings on production). Lightning `debug` is gated the same way.
- **Bulk security-advisory ingest:** drop OpenSSF / GHSA malware dumps (`security_advisory`, `@zalastax/nolb-*`, “Malicious code in @…”) on `CreateDocument`, `EditDocument`, P2P file receive, inventory resync, and `recordActivity`. JSON arrays of advisory objects recurse (cap 32). Those Internal-message floods OOMed production Hub. Pair with `@fabric/http` Internal-log + commit snapshot cuts.
- **Chat shoutbox:** WebRTC → Fabric TCP `P2P_RELAY` inner chat is UTF-8 text (legacy JSON `{ object.content }` envelopes are unpacked via `@fabric/core/functions/fabricChatText`). Registry marks `P2P_CHAT_MESSAGE` as `utf8-text`; author is AMP signature.
- **Identity paths:** Hub re-exports `fabricIdentityAccountPath` (core helper when the pin exports it). Receive leaf stays `fabricIdentityDerivationPath`.
- **Identity cluster:** ingest keys must be compressed or x-only hex (`pubkeyXOnly`); colon-smashed strings are `invalid pubkey`.
- **Beacon:** Hub ready-round retry tests match core recovered-witness checks (real Schnorr; fake `'00'` is rejected).
- **Device link:** per-origin create quota is in `@fabric/http` (`MAX_SESSIONS_PER_ORIGIN`); Hub re-exports it from `functions/fabricDeviceLink.js`. Browser device-link fetch omits client-set Origin/Referer.
- **Site login:** expired `GET /sessions/:delegationToken` requires matching `Authorization: Bearer` (http pin).
- **Pins:** `@fabric/core` lockfile `9938917804e2bf5ba5cf1fab7bf0975129d9063f` (Filesystem publish retain cut + production follow-ups from [#185](https://github.com/FabricLabs/fabric/pull/185); MuSig2 `autoAccept` default off, BIP-21 `req-*`, collection cwd-containment, Codacy `Number('…')` literals; plus MuSig2/BIP helpers, IPv6 candidate `[host]:port`, generic-message debug exact-match, exported `fabricIdentityAccountPath`, AMP wire name wins over inventory JSON `type: 98`); `@fabric/http` lockfile `7d7f1c7c918dabe7f2ae59d638b16e1a020d08bd` (POST-null 400, device-link DELETE cancel `ok: false`, 402 blob-id omit, CLI `fabricHomeEnv` before `Environment.start()`, Internal-log/commit snapshot cut, `fabricChatText` re-export, `pubkey@` strip + dedicated-NIC `:7778`→`:7777`, unicast `FABRIC_INTERFACE` sibling-NIC self-filter, Bearer path-token). `package.json` stays on `#feature/rsi`. `report:install` removes `package-lock.json` then `npm i --allow-git=all`. `scripts/hub.js` loads `~/.fabric/env` via `fabricHomeEnv` (process env wins). Playnet `loadPeerKeySettings` rethrows unless `MODULE_NOT_FOUND`; fallback keeps raw `FABRIC_SEED` hex as `{ seed }`.
- **Managed Bitcoin:** Hub RPC probe keeps cookie credentials (no longer a port-only stub that 401s an orphan `bitcoind`). Spawn early-exit / datadir lock **attaches** to the live node instead of `pm2` crash-looping. Shutdown kills only a Core this process spawned.
- **Playnet:** `npm run playnet:status -- --production` reports native `fabric-beacon` plus an optional sibling GoonCitizen contract id. Deploy the application namespace from GoonCitizen: `npm run playnet:deploy-gooncitizen -- --production --accept`.
- **SPA assets:** `assets/scripts/assets/manifest.json` loads the same-origin `/bundles/browser.min.js` (no remote `fabric.pub` script URI).

- **Document Market (opt-in):** Accumulate peer document inventories (`FABRIC_DOCUMENT_MARKET_ACCUMULATE=1`) and republish **held** files at a markup (`FABRIC_DOCUMENT_MARKET_REPUBLISH=1`, default +10%). Offer book on Documents; RPC `ListDocumentOffers` / `RefreshDocumentMarket`; reusable helper `@fabric/hub/functions/documentInventoryMarket`. Outbound inventory still lists only local blobs. See [PAYMENTS_PROTOCOL.md](PAYMENTS_PROTOCOL.md) phase **J**.
- **Dependencies / security:** Align with `@fabric/http` / `@fabric/core` Node **24.15.0**. Bump `ws@8.21.2`, `webpack@5.109.2`, `webpack-dev-server@5.2.6`, `nodemailer@9.0.4`, `electron@39.8.10`, `electron-builder@26.15.7`, `react-router-dom@7.18.2` (drop unused `Switch` imports); add direct `terser-webpack-plugin` for the webpack config. Overrides: `ws`, `uuid@11.1.1`, `tar@7.5.22`, `builder-util-runtime@9.7.0`. See [AUDIT.md](AUDIT.md) (RSC CSRF residual accepted for SPA `BrowserRouter`).
- **Browser bundle / React Router 7:** Webpack `conditionNames` no longer prefer `import` (which selected react-router `.mjs` and crashed the SPA with `__webpack_modules__[id].call is not a function`). Alias CJS entrypoints for `react-router` / `react-router-dom`; `BrowserRouter` uses RR7 `useTransitions` instead of obsolete v6 `future` flags.
- **npm git deps:** `.npmrc` / `report:install` use **`allow-git=all`** so nested Hub→http→core git preparation (commit SHAs) is not refused under npm 12+.
- **UI (Fomantic / Semantic):** The packaged site theme is renamed from **`sensemaker`** to **`fabric`**. **Arvo** `.woff2` are maintained via `@fabric/http` `make:fonts` / `build:semantic`; Hub uses `npm run build:semantic` (delegates to the dependency). The old `themes/sensemaker` tree was removed.
- **Document market × L1 index:** Auto-publish **Bitcoin block** (+ optional **tx**) Fabric documents with **immutable** canonical JSON (**schemaVersion 3**) for stable inventory HTLC / `DocumentPublish` preimages; default **`purchasePriceSats`**; **prune-aware** local catalog when `bitcoind` is pruned; Bitcoin status **`bitcoinPruned`** / **`bitcoinPruneHeight`**; roadmap [docs/PRODUCTION_ROADMAP.md](docs/PRODUCTION_ROADMAP.md), protocol [PAYMENTS_PROTOCOL.md](PAYMENTS_PROTOCOL.md) phase **I**.
- **Settings / Bitcoin UX:** Identity vs Hub wallet callouts on Settings (Bitcoin wallet) and Bitcoin page; **operator deposits checklist** when L1 is up; **Hub Bitcoin backend unavailable** when upstream is configured but RPC is down. **Backup** line links to Security.
- **Settings:** `FABRIC_BITCOIN_ENABLE=false` skips the Bitcoin service (headless E2E without Core). **`FABRIC_PORT`** coerced to a number in `settings/local.js` so Fabric Peer binds to the intended port.
- **CI:** Workflow **E2E RC** (`.github/workflows/e2e-rc.yml`) — browser E2E + `scripts/ci-e2e-payjoin.sh` on tags `v*` and manual dispatch; isolated `FABRIC_HUB_USER_DATA`, `FABRIC_PORT=17777`, `FABRIC_BITCOIN_ENABLE=false` for Payjoin script.
- **Settings:** Fix `/settings` crash (undefined Peers gate variable); align with `TopPanel` using `readHubAdminTokenFromBrowser()` as `hasHubAdminPeerNav`.
- **Roadmap:** [docs/PRODUCTION_ROADMAP.md](docs/PRODUCTION_ROADMAP.md) — phased plan for wallet, crowdfunds/federation, Payjoin+Lightning; production feature-flag JSON example.
- **Home / Bitcoin:** **Treasury** shortcut on Home → Bitcoin page anchor `#fabric-bitcoin-payjoin` (or Lightning when Payments flag off); Payjoin segment `id="fabric-bitcoin-payjoin"` for deep links.
- **Federation settings:** `id="settings-federation-heading"` on Distributed federation page for tests and anchors.
- **Admin / operations:** Added worker queue API + Admin controls (`GetWorkerStatus`, `ListWorkerQueue`, `RunWorkerQueueNow`, `DropWorkerQueueItem`, `ClearWorkerQueue`) and a node-wealth panel (`GetNodeWealthSummary`) showing wallet balance and labeled earning flows.

## [0.1.0-RC1] — 2026-03-20
- **Release engineering:** `npm run ci`, [docs/PRODUCTION.md](docs/PRODUCTION.md), [docs/MARKETING_OVERVIEW.md](docs/MARKETING_OVERVIEW.md), [docs/RELEASE_CHECKLIST.md](docs/RELEASE_CHECKLIST.md), GitHub Actions CI, README positioning.
- **Payments / documents:** Inventory HTLC preimage bound to canonical **`DocumentPublish`** envelope via **`@fabric/core/functions/publishedDocumentEnvelope`** (Hub [loader](functions/publishedDocumentEnvelope.js)); AES-GCM phase-2 delivery; Bridge chunk assembly + preimage unlock UI.
- **Document list:** `ListDocuments` adds **`storageL1Status`** (batched Bitcoin RPC: one mempool query + parallel `getrawtransaction` per distinct storage-contract txid); Documents UI shows mempool / confirmation depth beside the Storage contract link.
- **L1 payment verify:** **`GET /services/bitcoin/transactions/:txid?address=&amountSats=`** (consolidated into the transaction resource; no separate `.../payment-verification` path); removed **`POST .../verify-payment`** and **`POST .../payments/verify-l1`**. `verifyL1Payment` in **`functions/bitcoinClient.js`**; **`/services/bitcoin/resources`** UI.
- **Inventory HTLC on-chain claim:** Admin JSON-RPC **`ClaimInventoryHtlcOnChain`** — tapscript-path PSBT, sign with hub identity key, `sendrawtransaction`; helpers in `functions/inventoryHtlc.js`; `GetInventoryHtlcSellerReveal` returns **`claimTxid`** when present.
- **REST surface:** Payjoin create session is **POST `/services/bitcoin/payjoin/sessions`** (replaces **`/deposits`**); submit proposal is **POST `.../sessions/:id/proposals`** (replaces **`.../proposal`**). Lightning channel close is **DELETE `/services/lightning/channels/:channelId`** (replaces **POST `.../channels/close`**). `functions/bitcoinClient.js` and **`/services/bitcoin/resources`** updated; legacy **`/lightning/invoice|pay|decode`** fallbacks removed in favor of **`/invoices`**, **`/payments`**, **`/decodes`**.
- Fill in additional user-facing changes per commit history when publishing the next RC.

[0.1.0-RC1]: https://github.com/FabricLabs/hub.fabric.pub/compare/main...v0.1.0-RC1
